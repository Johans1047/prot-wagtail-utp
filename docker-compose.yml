# services:
#   # Django/Wagtail Application
#   web:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: jic_wagtail
#     command: python mysite/manage.py runserver 0.0.0.0:8000
#     volumes:
#       - ./mysite:/app/mysite
#       - static_files:/app/mysite/staticfiles
#       # - media_files:/app/media
#     ports:
#       - "8000:8000"
#     environment:
#       - DEBUG=True
#       - PYTHONUNBUFFERED=1
#       - DATABASE_URL=postgresql://jicuser:jicpass@db:5432/jic_db
#       - MINIO_ENDPOINT=minio:9000
#       - MINIO_ACCESS_KEY=minioadmin
#       - MINIO_SECRET_KEY=minioadmin
#       - MINIO_USE_SSL=false
#       - MINIO_BUCKET_NAME=jic-media
#     depends_on:
#       db:
#         condition: service_healthy
#       minio:
#         condition: service_healthy
#     networks:
#       - jic_network

#   # Django LiveReload Server
#   livereload:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: jic_livereload
#     command: python mysite/manage.py livereload --host=0.0.0.0 --port=35729
#     volumes:
#       - ./mysite:/app/mysite
#     ports:
#       - "35729:35729"
#     environment:
#       - DEBUG=True
#       - PYTHONUNBUFFERED=1
#       - DATABASE_URL=postgresql://jicuser:jicpass@db:5432/jic_db
#     depends_on:
#       - web
#     networks:
#       - jic_network

#   # TailwindCSS Watcher
#   # tailwind:
#     build:
#       context: .
#       dockerfile: Dockerfile.node
#     container_name: jic_tailwind
#     command: sh -c "npx tailwindcss -i /app/mysite/theme/static/src/input.css -o /app/mysite/theme/static/src/output.css --watch"
#     volumes:
#       - ./mysite:/app/mysite
#       - ./tailwind.config.js:/app/tailwind.config.js
#       - node_modules:/app/node_modules
#     networks:
#       - jic_network
#     depends_on:
#       - web
#     restart: unless-stopped
#     stdin_open: true
#     tty: true

#   # PostgreSQL Database
#   db:
#     image: postgres:15-alpine
#     container_name: jic_postgres
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=jic_db
#       - POSTGRES_USER=jicuser
#       - POSTGRES_PASSWORD=jicpass
#     ports:
#       - "5432:5432"
#     networks:
#       - jic_network
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U jicuser -d jic_db"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

#   # MinIO Object Storage
#   minio:
#     image: minio/minio:latest
#     container_name: jic_minio
#     command: server /data --console-address ":9001"
#     volumes:
#       - minio_data:/data
#     environment:
#       - MINIO_ROOT_USER=minioadmin
#       - MINIO_ROOT_PASSWORD=minioadmin
#     ports:
#       - "9000:9000"  # API
#       - "9001:9001"  # Console
#     networks:
#       - jic_network
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
#       interval: 30s
#       timeout: 20s
#       retries: 3

#   # MinIO Client - Para crear bucket automáticamente
#   minio-client:
#     image: minio/mc:latest
#     container_name: jic_minio_client
#     depends_on:
#       minio:
#         condition: service_healthy
#     entrypoint: >
#       /bin/sh -c "
#       /usr/bin/mc alias set myminio http://minio:9000 minioadmin minioadmin;
#       /usr/bin/mc mb myminio/jic-media --ignore-existing;
#       /usr/bin/mc anonymous set download myminio/jic-media;
#       exit 0;
#       "
#     networks:
#       - jic_network
#     restart: "no"

# volumes:
#   postgres_data:
#     driver: local
#   minio_data:
#     driver: local
#   static_files:
#     driver: local
#   media_files:
#     driver: local
#   node_modules:
#     driver: local

# networks:
#   jic_network:
#     driver: bridge

services:

  # jicweb_app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.prod
  #   container_name: jicweb_app
  #   command: >
  #     gunicorn
  #     --chdir /app/mysite
  #     --config /app/gunicorn_config.py
  #     mysite.wsgi:application
  #   volumes:
  #     - ./jic/mysite:/app/mysite
  #     - static_files:/app/mysite/staticfiles
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DJANGO_ALLOWED_HOSTS=${DJANGO_ALLOWED_HOSTS}
  #     - DJANGO_SECRET_KEY=${DJANGO_SECRET_KEY}
  #     - DEBUG=False
  #     - PYTHONUNBUFFERED=${PYTHONUNBUFFERED}
  #     - DB_REPLICATION_ENABLED=${DB_REPLICATION_ENABLED}
  #     - DB_NAME=${DB_NAME}
  #     - DB_USER=${DB_USER}
  #     - DB_PASSWORD=${DB_PASSWORD}
  #     - DB_HOST=${DB_HOST}
  #     - DB_PORT=${DB_PORT}
  #     - DB_SSLMODE=${DB_SSLMODE}
  #     - DB_CHANNEL_BINDING=${DB_CHANNEL_BINDING}
  #     - MINIO_ENDPOINT=${MINIO_ENDPOINT}
  #     - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
  #     - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
  #     - MINIO_USE_SSL=${MINIO_USE_SSL}
  #     - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
  #     # Gunicorn
  #     - GUNICORN_WORKERS=${GUNICORN_WORKERS}
  #     - GUNICORN_THREADS=${GUNICORN_THREADS}
  #     - GUNICORN_WORKER_CLASS=${GUNICORN_WORKER_CLASS}
  #     - GUNICORN_MAX_REQUESTS=${GUNICORN_MAX_REQUESTS}
  #     - GUNICORN_MAX_REQUESTS_JITTER=${GUNICORN_MAX_REQUESTS_JITTER}
  #     - GUNICORN_TIMEOUT=${GUNICORN_TIMEOUT}
  #     - GUNICORN_GRACEFUL_TIMEOUT=${GUNICORN_GRACEFUL_TIMEOUT}
  #     - GUNICORN_BIND=${GUNICORN_BIND}
  #     - GUNICORN_ACCESS_LOG=${GUNICORN_ACCESS_LOG}
  #     - GUNICORN_ERROR_LOG=${GUNICORN_ERROR_LOG}
  #   depends_on:
  #     jic_minio_storage:
  #       condition: service_healthy
  #   restart: unless-stopped
  #   networks:
  #     - netX


  jicweb_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jicweb_app
    command: python mysite/manage.py runserver 0.0.0.0:8000
    volumes:
      - ./jic/mysite:/app/mysite
      - static_files:/app/mysite/staticfiles
    ports:
      - "8000:8000"
    environment:
      - DEBUG=${DEBUG}
      - TAILWIND_WATCH=0
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED}
      - DB_REPLICATION_ENABLED=${DB_REPLICATION_ENABLED}
      # PostgreSQL connection (jicweb_master container) - using postgres user
      - POSTGRESQL_DATABASE=${POSTGRESQL_DATABASE}
      - POSTGRESQL_POSTGRES_USERNAME=${POSTGRESQL_POSTGRES_USERNAME}
      - POSTGRESQL_POSTGRES_PASSWORD=${POSTGRESQL_POSTGRES_PASSWORD}
      - POSTGRESQL_HOST=${POSTGRESQL_HOST}
      - POSTGRESQL_DATABASE_PORT_NUMBER=${POSTGRESQL_DATABASE_PORT_NUMBER}
      # - JIC_PROJECTS_API_URL=${JIC_PROJECTS_API_URL:-http://192.168.140.114:8000/api/proyectos-jic}
      - JIC_PROJECTS_API_URL=${JIC_PROJECTS_API_URL:-http://jic_projects_mock_api:8001/api/proyectos-jic}
      - JIC_PROJECTS_API_TIMEOUT=${JIC_PROJECTS_API_TIMEOUT:-3}
      # - DB_OPTIONS=-c search_path=public
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
    depends_on:
      jic_minio_storage:
        condition: service_healthy
    networks:
      - netX

  jicweb_app_livereload:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jicweb_app_livereload
    command: python mysite/manage.py livereload --host=0.0.0.0 --port=35729
    volumes:
      - ./jic/mysite:/app/mysite
    ports:
      - "35729:35729"
    environment:
      - DEBUG=${DEBUG}
      - TAILWIND_WATCH=0
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED}
      # PostgreSQL connection (jicweb_master container) - using postgres user
      - POSTGRESQL_DATABASE=${POSTGRESQL_DATABASE}
      - POSTGRESQL_POSTGRES_USERNAME=${POSTGRESQL_POSTGRES_USERNAME}
      - POSTGRESQL_POSTGRES_PASSWORD=${POSTGRESQL_POSTGRES_PASSWORD}
      - POSTGRESQL_HOST=${POSTGRESQL_HOST}
      - POSTGRESQL_DATABASE_PORT_NUMBER=${POSTGRESQL_DATABASE_PORT_NUMBER}
      - JIC_PROJECTS_API_URL=${JIC_PROJECTS_API_URL:-http://jic_projects_mock_api:8001/api/proyectos-jic}
      - JIC_PROJECTS_API_TIMEOUT=${JIC_PROJECTS_API_TIMEOUT:-3}
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
    depends_on:
      - jicweb_app
    networks:
      - netX

  jic_projects_mock_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jic_projects_mock_api
    command: >
      sh -c "pip install --no-cache-dir -r /app/mock_projects_api/requirements.txt && uvicorn mock_projects_api.app:app --host 0.0.0.0 --port 8001"
    volumes:
      - ./mock_projects_api:/app/mock_projects_api
    ports:
      - "8001:8001"
    environment:
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED}
    networks:
      - netX

  jic_projects_sync_worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jic_projects_sync_worker
    command: >
      sh -c "
        INTERVAL=$${JIC_PROJECTS_SYNC_INTERVAL:-60};
        echo \"[projects-sync] Interval: $${INTERVAL}s (weekly)\";
        while true; do
          TS=$$(date -u +%Y-%m-%dT%H:%M:%SZ);
          echo \"[$$TS] Running API->DB sync...\";
          python mysite/manage.py sync_projects_api --skip-checks || echo \"[$$TS] Sync failed; retrying next cycle.\";
          sleep $${INTERVAL};
        done
      "
    volumes:
      - ./jic/mysite:/app/mysite
    environment:
      - DEBUG=${DEBUG}
      - PYTHONUNBUFFERED=${PYTHONUNBUFFERED}
      - DB_REPLICATION_ENABLED=${DB_REPLICATION_ENABLED}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_SSLMODE=${DB_SSLMODE}
      - DB_CHANNEL_BINDING=${DB_CHANNEL_BINDING}
      - JIC_PROJECTS_API_URL=${JIC_PROJECTS_API_URL:-http://jic_projects_mock_api:8001/api/proyectos-jic}
      - JIC_PROJECTS_API_TIMEOUT=${JIC_PROJECTS_API_TIMEOUT:-3}
      - JIC_PROJECTS_AUTO_SYNC_DB=${JIC_PROJECTS_AUTO_SYNC_DB:-1}
      - JIC_PROJECTS_SYNC_INTERVAL=${JIC_PROJECTS_SYNC_INTERVAL:-60}
      - MINIO_ENDPOINT=${MINIO_ENDPOINT}
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
      - MINIO_USE_SSL=${MINIO_USE_SSL}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
    depends_on:
      jicweb_master:
        condition: service_healthy
      jic_projects_mock_api:
        condition: service_started
    restart: unless-stopped
    networks:
      - netX

  jicweb_master:
    image: 'bitnamilegacy/postgresql:17'
    ports:
      - '5440:5432'
    networks:
      netX:
        ipv4_address: 172.20.1.85
    volumes:
      - 'jicweb_master_data_v1:/bitnami/postgresql'
    environment:
      - POSTGRESQL_REPLICATION_MODE=master
      - POSTGRESQL_POSTGRES_USERNAME=${POSTGRESQL_POSTGRES_USERNAME}
      - POSTGRESQL_POSTGRES_PASSWORD=${POSTGRESQL_POSTGRES_PASSWORD}
      - POSTGRESQL_DATABASE=${POSTGRESQL_DATABASE}
      - POSTGRESQL_USERNAME=${POSTGRESQL_USERNAME}
      - POSTGRESQL_PASSWORD=${POSTGRESQL_PASSWORD}
      - POSTGRESQL_REPLICATION_USER=${DB_REPLICATION_USER}
      - POSTGRESQL_REPLICATION_PASSWORD=${DB_REPLICATION_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mast_jicweb -d db_jicweb"]
      interval: 10s
      timeout: 5s
      retries: 5

  jicweb_slave:
    image: 'bitnamilegacy/postgresql:17'
    ports:
      - '5432'
    networks:
      - netX
    depends_on:
      jicweb_master:
        condition: service_healthy
    environment:
      - POSTGRESQL_REPLICATION_MODE=slave
      - POSTGRESQL_REPLICATION_USER=${DB_REPLICATION_USER}
      - POSTGRESQL_REPLICATION_PASSWORD=${DB_REPLICATION_PASSWORD}
      - POSTGRESQL_MASTER_HOST=${POSTGRESQL_HOST}
      - POSTGRESQL_PASSWORD=${POSTGRESQL_PASSWORD}
      - POSTGRESQL_MASTER_PORT_NUMBER=${POSTGRESQL_DATABASE_PORT_NUMBER}
  jic_minio_storage:
    restart: "no"
    image: bitnami/minio:latest
    ports:
      - 9010:9000
      - 9011:9001
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_CONSOLE_ADDRESS=:9001
      - MINIO_DEFAULT_BUCKETS=${MINIO_BUCKET_NAME}:public
      - MINIO_SCHEME=http
      - MINIO_SKIP_CLIENT=no
      - TZ=UTC
    networks:
      netX:
        ipv4_address: 172.20.0.75
    volumes:
      - 'jicst_storage_data:/bitnami/minio/data'
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/minio/health/live || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  jic_minio_backup:
    image: bitnami/minio:latest
    entrypoint: |
      /bin/bash -c "
        mc alias set src http://172.20.0.75:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD};
        while true; do
          TIMESTAMP=$$(date +%Y%m%d_%H%M%S);
          echo \"[$$TIMESTAMP] Iniciando backup de MinIO...\";
          mc mirror --overwrite src/${MINIO_BUCKET_NAME} /bitnami/minio/backups/$$(date +%Y%m%d)/ || echo \"[$$TIMESTAMP] ERROR: Falló el backup\";
          echo \"[$$TIMESTAMP] Backup completado en /bitnami/minio/backups/$$(date +%Y%m%d)/\";
          sleep 86400;
        done
      "
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_BUCKET_NAME=${MINIO_BUCKET_NAME}
    volumes:
      - jicst_backup_data:/bitnami/minio/backups
    depends_on:
      jic_minio_storage:
        condition: service_healthy
    networks:
      - netX
    restart: unless-stopped

  jic_db_backup:
    image: bitnamilegacy/postgresql:17
    entrypoint: |
      /bin/bash -c "
        export PGPASSWORD=1*o63U9JlN5bLp;
        while true; do
          TIMESTAMP=$$(date +%Y%m%d_%H%M%S);
          BACKUP_FILE=/bitnami/postgresql/backups/$$(date +%Y%m%d)/db_jicweb_$$TIMESTAMP.sql.gz;
          mkdir -p /bitnami/postgresql/backups/$$(date +%Y%m%d);
          echo \"[$$TIMESTAMP] Iniciando backup de PostgreSQL...\";
          pg_dump -h 172.20.1.85 -U mast_jicweb -d db_jicweb | gzip > $$BACKUP_FILE || echo \"[$$TIMESTAMP] ERROR: Falló el backup\";
          echo \"[$$TIMESTAMP] Backup completado: $$BACKUP_FILE\";
          find /bitnami/postgresql/backups -name '*.sql.gz' -mtime +30 -delete;
          sleep 86400;
        done
      "
    volumes:
      - jicweb_master_data_backup_v1:/bitnami/postgresql/backups
    depends_on:
      jicweb_master:
        condition: service_healthy
    networks:
      - netX
    restart: unless-stopped

volumes:
  jicst_storage_data:
    driver: local
  jicst_backup_data:
    driver: local
  jicweb_master_data_v1:
    driver: local
  jicweb_master_data_backup_v1:
    driver: local
  static_files:
    driver: local

networks:
  netX:
    name: netX
    external: true

# services:
#   jicweb_app:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: jicweb_app
#     command: python mysite/manage.py runserver 0.0.0.0:8000
#     volumes:
#       - ./mysite:/app/mysite
#       - static_files:/app/mysite/staticfiles
#       # - media_files:/app/media
#     ports:
#       - "8000:8000"
#     environment:
#       - DEBUG=True
#       - PYTHONUNBUFFERED=1
#       - DATABASE_URL=postgresql://jicuser:jicpass@db:5432/jic_db
#       - MINIO_ENDPOINT=minio:9000
#       - MINIO_ACCESS_KEY=minioadmin
#       - MINIO_SECRET_KEY=minioadmin
#       - MINIO_USE_SSL=false
#       - MINIO_BUCKET_NAME=jic-media
#     depends_on:
#       jicweb_master:
#         condition: service_healthy
#       jic_minio_storage:
#         condition: service_healthy
#     networks:
#       - netX

#   jicweb_master:
#     image: 'bitnamilegacy/postgresql:17'
#     ports:
#       - '5440:5432'
#     networks:
#       netX:
#         ipv4_address: 172.20.1.85
#     volumes:
#       - 'jicweb_master_data_v1:/bitnami/postgresql'
#     environment:
#       - POSTGRESQL_REPLICATION_MODE=master
#       - POSTGRESQL_POSTGRES_PASSWORD=1*o63U9JlN5bLp
#       - POSTGRESQL_REPLICATION_USER=repl_jicweb
#       - POSTGRESQL_REPLICATION_PASSWORD=2U552^2UP?yU]>
#       - POSTGRESQL_USERNAME=mast_jicweb
#       - POSTGRESQL_PASSWORD=g0hNw,0p1MvY
#       - POSTGRESQL_DATABASE=db_jicweb

#   jicweb_slave:
#     image: 'bitnamilegacy/postgresql:17'
#     ports:
#       - '5432'
#     networks:
#       - netX
#     depends_on:
#       - jicweb_master
#     environment:
#       - POSTGRESQL_REPLICATION_MODE=slave
#       - POSTGRESQL_REPLICATION_USER=repl_jicweb
#       - POSTGRESQL_REPLICATION_PASSWORD=2U552^2UP?yU]>
#       - POSTGRESQL_MASTER_HOST=172.20.1.85
#       - POSTGRESQL_PASSWORD=g0hNw,0p1MvY
#       - POSTGRESQL_MASTER_PORT_NUMBER=5432

#   jic_minio_storage:
#     restart: "no"
#     image: bitnamilegacy/minio:latest
#     ports:
#       - 9010:9000
#       - 9011:9001
#     environment:
#       - MINIO_ROOT_USER=mast_st_jic
#       - MINIO_ROOT_PASSWORD=KtWzuaY3Uy8F
#       # - MINIO_ACCESS_KEY=mast_st_jic
#       # - MINIO_SECRET_KEY=KtWzuaY3Uy8F
#       - MINIO_CONSOLE_ADDRESS=:9001
#       - TZ=UTC
#     networks:
#       netX:
#         ipv4_address: 172.20.0.75
#     volumes:
#       - 'jicst_storage_data:/bitnami/minio/data'

# volumes:
#   jicst_storage_data:
#     driver: local
#   jicweb_master_data_v1:
#     driver: local

# networks:
#   netX:
#     name: netX
#     external: true